# This file is generated by the dot_env cookbook. Update it by setting the
# stack's custom JSON and executing the dot_env::update recipe.

<% if @env %>
  <% @env.sort.each do |key, val| %>
  <%= key.to_s %>: <%= (val && val.to_s.inspect) %>
  <% end %>
<% end %>
